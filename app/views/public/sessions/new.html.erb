<h4>ログイン</h4>
<h5>会員の方はこちらからログイン</h5>

<%= form_with model: resource, scope: resource_name, url: session_path(resource_name) do |f| %>
  <div class="field">
    <%= f.label :メールアドレス %>
    <%= f.email_field :email, placeholder: "sample@example.com" %>
  </div>

  <div class="field">
    <%= f.label :パスワード %>
    <%= f.password_field :password %>
  </div>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
  
<% end %>

<div>
<h5>会員登録がお済みでない方</h5>
<h6><%= render "public/shared/links" %></h6>
</div>

